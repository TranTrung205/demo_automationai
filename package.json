{
  "name": "ai-test-agent",
  "version": "2.0.0",
  "type": "module",

  "scripts": {
    "agent": "tsx scripts/run-v10.ts",
    "agent:debug": "tsx scripts/debug-agent.ts",

    "planner": "tsx scripts/test-planner.ts",

    "test": "playwright test",
    "test:debug": "playwright test --debug",

    "report": "playwright show-report",

    "clean:report": "rimraf ai-report"
  },

  "dependencies": {
    "yaml": "^2.8.2"
  },

  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.2.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}